# 探星阁内容中心使用说明

在线后台：<https://tanxg-content-center.sanity.studio/>

Mac 用户可双击 `启动内容编辑器.command`，Windows 用户可双击 `启动内容编辑器.bat`。也可以直接在浏览器中打开上面的在线后台。

## 日常编辑

1. 使用获邀加入 TanXG 项目的 Sanity 账号登录。
2. 在左侧选择要修改的栏目，例如“课程检索”“课程讲义”“科学研究”或“团队成员”。
3. 新建或打开一条内容，填写后点击 **Publish（发布）**。
4. 刷新网站页面即可看到已发布内容。没有发布的草稿不会出现在公开网站上。

## 多人协作规则

- 不同成员可以同时编辑不同内容，不会再因为修改同一个 HTML 文件而互相覆盖。
- 同一条内容被多人同时打开时，后台会同步显示对方的修改状态。
- 新增、删除或修改文章、课程、讲义、成员等日常内容，统一在内容中心完成。
- 页面布局、颜色、动画和程序功能仍属于网站代码，需要通过 GitHub Desktop 协作。

## 网站的数据来源

网站优先读取 Sanity 中已经发布的内容；如果网络暂时不可用，会保留本地静态内容作为备用，不会出现整页空白。

当前 Sanity 项目为 `TanXG`，项目 ID 为 `o9vxg32e`，使用 `production` 数据集。
